####################### This file is generated by chef. Don't make manual changes.###################################

<VirtualHost *:80>
        DocumentRoot /data/futuredekho/production/public_html
        ServerName futuredekho.com
	ServerAlias futuredekho.com www.futuredekho.com webmail.futuredekho.com admin.futuredekho.com
	ScriptAlias /cgi-bin/ /home/futuredekho/production/cgi-bin/
	ScriptAlias /awstats/ /home/futuredekho/production/cgi-bin/
	DirectoryIndex index.html index.htm index.php index.php4 index.php5
        <Directory /data/futuredekho/production/public_html>
		Options -Indexes +IncludesNOEXEC +SymLinksIfOwnerMatch +ExecCGI
                AllowOverride All
		Require all granted
        </Directory>

	<Directory /home/futuredekho/production/cgi-bin>
		AllowOverride All
                Require all granted
		AllowOverride All Options=ExecCGI,Includes,IncludesNOEXEC,Indexes,MultiViews,SymLinksIfOwnerMatch
	</Directory>
	RewriteEngine on
	RewriteCond %{HTTP_HOST} =webmail.futuredekho.com
	RewriteRule ^(.*) https://futuredekho.com:20000/ [R]
	RewriteCond %{HTTP_HOST} =admin.futuredekho.com
	RewriteRule ^(.*) https://futuredekho.com:10000/ [R]
	<Files awstats.pl>
		AuthName "futuredekho.com statistics"
		AuthType Basic
		AuthUserFile /data/futuredekho/production/.awstats-htpasswd
		require valid-user
	</Files>
	Alias /dav /data/futuredekho/production/public_html
	<Location /dav>
		DAV on
		AuthType Basic
		AuthName "futuredekho.com"
		AuthUserFile /data/futuredekho/production/etc/dav.digest.passwd
		Require valid-user
		ForceType text/plain
		Satisfy All
	</Location>

	RewriteEngine on
        	RewriteCond %{HTTP_HOST} !^www\.futuredekho\.com$ [NC]
        	RewriteRule ^(.*)$ http://www.futuredekho.com$1 [R=301,L]
        ErrorLog /var/log/httpd/futuredekho.com-error.log
        CustomLog  /var/log/httpd/futuredekho.com-access_log common
</VirtualHost>
